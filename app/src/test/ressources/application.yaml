spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/reservation_vaccination_main
    username: postgres
    password: Admin
  
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate.ddl-auto: create