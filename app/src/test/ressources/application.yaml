spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vaccinationtest
    username: postgre
    password: Admin
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  sql:
    init:
      mode: always
  server:
    port: 8081